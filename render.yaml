services:
  - type: web
    name: chungkhoanvn
    runtime: python
    buildCommand: pip install -r requirements.txt && python download_fonts.py
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    autoDeploy: true
